# Hoist transitive deps to the root node_modules (npm-like layout).
# The API imports a few packages directly that are only transitive in the
# pnpm strict layout (e.g. `express` via @nestjs/platform-express). Hoisting
# makes them resolvable at runtime without editing the lockfile.
shamefully-hoist=true
