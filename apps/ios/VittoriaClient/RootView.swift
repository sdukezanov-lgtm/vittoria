import SwiftUI

/// Placeholder root view replaced in PART 2 with the real navigation stack.
struct RootView: View {
    var body: some View {
        Text("VITTORIA HOME")
            .font(.headline)
    }
}

#Preview {
    RootView()
}
