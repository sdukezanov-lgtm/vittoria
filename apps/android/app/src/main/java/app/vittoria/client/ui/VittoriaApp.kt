package app.vittoria.client.ui

import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import app.vittoria.client.di.AppContainer

/**
 * Root composable — placeholder for PART 1.
 *
 * PART 2 will replace this with a NavHost and the full screen hierarchy.
 */
@Composable
fun VittoriaApp(container: AppContainer) {
    MaterialTheme {
        Text("VITTORIA HOME")
    }
}
